name: metreyar_flutter_web
description: A new Flutter web project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  go_router: ^13.2.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  fonts:
    - family: Vazirmatn
      fonts:
        - asset: fonts/Vazirmatn-Regular.ttf

  assets:
    - assets/
    - icons/
    - uploads/
dependencies:
  flutter:
    sdk: flutter
  google_ml_kit: ^0.6.0
  image_picker: ^0.8.5+3 # برای انتخاب تصویر از گالری یا دوربین
